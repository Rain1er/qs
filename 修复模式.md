# 基于全部 2576 个 case_id 的 SSRF 修复模式分析

## 方法说明

这次分析口径不再是“只看保留的 Java case”，而是先处理 [outputs/java_direct_ssrf_trace.json](/workspaces/Quick_start/outputs/java_direct_ssrf_trace.json) 中全部 `2576` 个 `case_id` 及其全部 `references`，再从这个全集里收敛出能够支持 Java Direct SSRF 修复模式结论的子集。

本轮我额外生成了一个全量 reference 审核产物：
- [outputs/all_case_reference_review.json](/workspaces/Quick_start/outputs/all_case_reference_review.json)

这个文件对 `2576` 个 case 全部给出：
- `case_id`
- `classification`
- `reference_count`
- `reference_type_counts`
- `top_domains`
- `references`

也就是说，这一轮“研究所有 case_id”不是一句口头承诺，而是有一个可回查的全集摘要文件。

### 全量 corpus 事实
- 总 case 数：`2576`
- 总 references 数：`10062`
- 去重后 unique references：`9112`
- 本地分类为 `java_direct_ssrf = yes` 的 case：`80`
- 其中 `keep = 16`
- 其余 `2496` 个 case 都不是当前任务要提炼的 Java Direct SSRF 修复模式主语料

### 全量 references 类型分布
- `advisory_artifact`: `4345`
- `downstream_propagation_artifact`: `4320`
- `code_artifact`: `836`
- `discussion_artifact`: `561`

### Java 相关子集分布
- `80` 个 `java_direct_ssrf = yes` case 的 references 合计：`391`
- 其中：
  - `advisory_artifact`: `157`
  - `downstream_propagation_artifact`: `165`
  - `code_artifact`: `27`
  - `discussion_artifact`: `42`

### 为什么不能直接从全集提模式
全集里最大桶是：
- `1300` 个 `java=no;evidence=low;repeat=low;keep=drop`
- `460` 个 `java=no;evidence=high;repeat=low;keep=drop`

这说明如果不先处理全部 case，而是直接从“看上去像 SSRF 的 case”里抽模式，很容易被非 Java、XXE、WordPress、传播公告主导的样本污染。  
因此，**正确流程是：先研究全部 2576 个 case 的 reference universe，再只从 Java Direct SSRF 且有代码证据的层里抽修复模式。**

## 全量 case reference review 概览

### 2576 个 case 的总体结论
- `2265` 个 case 的主因是：`drop: not confidently Java/Maven`
- `123` 个 case 的主因是：`drop: SSRF wording present but direct outbound request signal is weak`
- `100` 个 case 的主因是：`drop: XXE/XML parsing case, not direct SSRF`
- 真正进入 `java_direct_ssrf = yes` 的只有 `80` 个
- 在这 `80` 个里，仍有 `64` 个因为 reference 结构或代码证据不足而被降级为 `drop`
- 所以“能用于 Java SSRF 修复模式抽象”的主语料只剩 `16` 个 `keep` case

### 结论层级
基于全部 references 复核后，当前 corpus 里每个 case 的可用结论只能分成三档：

1. `strict-chain`
   所有 references 都支持“历史修复链 / follow-up / 明确 continuation”  
   当前 corpus 中，**没有** 一条 Java case family 能被稳妥地放进这一档。

2. `tightening-family`
   references 支持“重复收紧 / same family / later hardening / 连续安全补丁”，但不支持把后续 case 写成已证实的“前次修复被绕过”  
   当前最典型的是：
   - `OFBiz`
   - `Kylin`

3. `single-fix`
   references 只支持一次修复样本，或只有一次明确代码级修复  
   例如：
   - `EasyAdmin`
   - `Karaf`
   - `Jenkins`
   - `Spring AI`

### 16 个保留 case 的 reference review 结论

| 分层 | case_id 数量 | 允许的表述 |
|---|---:|---|
| `strict-chain` | 0 | 不使用 `Fix-1 -> Bypass-1 -> Fix-2` |
| `tightening-family` | 2 个 family | 只能写“连续收紧 / repeated fix family / later hardening” |
| `single-fix` | 其余保留 case | 只能写“单次修复模式样本” |

这里最重要的改变是：
- 旧版文档把 `OFBiz` 写得接近“已证实绕过链”
- 这版在全量 references 复核后，**主动降级**
- 因为 references 证明的是“连续 security fix + 防线收紧”，但没有足够直接文字把两次 CVE 串成“前次修复被后次漏洞明确绕过”的官方断言

## 可证实历史修复链

### 结论
在 `2576` 个 case 的全量 reference universe 中，当前 **没有** 一条 Java Direct SSRF family 能被严格写成：

```text
Fix-1 -> Bypass-1 -> Fix-2
```

换句话说：
- 当前全集能证明“连续安全修补”
- 能证明“后续收紧”
- 能证明“same repository repeated family”
- 但**还不足以只凭 references + 当前 trace 就把任何一条链写成已证实绕过链**

这不是分析不够，而是证据口径变严后的结果。

## 连续收紧但无法严格证明绕过

### Family 1：OFBiz

#### all references reviewed
- `GHSA-w8w4-463p-8pg7`
  - `NVD`
  - `OFBIZ-13132` JIRA
  - `lists.apache.org` thread
  - `ofbiz.apache.org/download.html`
  - `ofbiz.apache.org/security.html`
- `GHSA-qxp5-vjrm-298x`
  - `NVD`
  - `OFBIZ-13158` JIRA
  - `lists.apache.org` thread
  - `ofbiz.apache.org/download.html`
  - `ofbiz.apache.org/security.html`
  - `openwall`

#### reference summary
- `OFBIZ-13132` 直接写的是：`Add validation to screen/script URI to block URL patterns`
- `OFBIZ-13158` 直接写的是：`Update method to check if the string starts with component:// instead of merely containing it`
- JIRA 页面还能看出：
  - `OFBIZ-13132` 的 `Fix Version` 是 `18.12.16`
  - `OFBIZ-13158` 的 `Fix Version` 是 `18.12.17`
  - `OFBIZ-13158` 的描述是：`To work the component protocol must be placed at start of URL`
- 这些 references 一起证明的是：
  - 同一项目在相邻安全版本里连续收紧了同一类 URL 护栏
  - 后一轮修的是前一轮 guard 的语义精度
- 但这些 references **没有** 明确写出：
  - `CVE-2024-47208 is a bypass of CVE-2024-45507`
  - 或 `incomplete fix` / `follow-up to previous CVE`

#### code summary
- 第一轮 case `GHSA-w8w4-463p-8pg7`：
  - `GroovyUtil.getScriptClassFromLocation(...)` 在 `openStream()` 前增加 `UtilValidate.urlInString(...)`
- 第二轮 case `GHSA-qxp5-vjrm-298x`：
  - `UtilValidate.urlInString(...)` 从 `contains("component://")`
  - 收紧为 `startsWith("component://")`

#### chain confidence
- `tightening-family`

#### allowed phrasing
- 可以写：
  - “连续收紧链”
  - “guard semantic tightening”
  - “later hardening of the same validation path”
- 不应写：
  - “已证实前次修复被后次漏洞绕过”

#### reference evidence
- `discussion`: `OFBIZ-13132`, `OFBIZ-13158`, `lists.apache.org`, `openwall`
- `advisory`: `NVD`, `ofbiz security/download page`

#### code evidence
- `GroovyUtil.getScriptClassFromLocation(...)`
- `UtilValidate.urlInString(...)`
- `contains("component://") -> startsWith("component://")`

#### reference-aware 伪代码
```text
V0
resolved_target = resolver.normalize(input_url)
requester.fetch(resolved_target)

Fix-1
resolved_target = resolver.normalize(input_url)
if validator.urlInString(resolved_target):
    reject()
requester.fetch(resolved_target)

Later hardening / Follow-up tightening
validator.urlInString changes from:
    if "component://" in resolved_target: safe
to:
    if resolved_target startsWith "component://": safe

What the chain proves
the project moved from "block URL-like strings" to
"tighten the exact trusted-prefix semantics of that block"
```

### Family 2：Kylin

#### all references reviewed
- `GHSA-3v67-545x-ffc3`
  - `NVD`
  - commit
  - repo
  - `KYLIN-5644` JIRA
  - `lists.apache.org`
- `GHSA-f6m8-qm7j-fh65`
  - `NVD`
  - PR `#2332`
  - commit
  - repo
  - `KYLIN-6082` JIRA
  - `lists.apache.org`
  - `openwall`

#### reference summary
- `KYLIN-5644` 明确讨论的是：
  - `fix diag api security`
  - `encryption changed from base64 to AES`
  - 重点在 `EncryptUtil.encrypt/decrypt` 和参数传递
- `GHSA-f6m8-qm7j-fh65` 这一组 references 则把问题指向新的 SSRF case，但从当前可见 reference 里，**没有明确文本**把它写成：
  - “前一修复不完整”
  - 或 “后续 bypass”
- trace 里它们有 `repeat_fix_relation`
- 但在“all references first”的口径下，这个 relation 只能当线索，不直接升级成确定事实

#### code summary
- 第一轮修复点在 `NBasicController.decodeHost(...)`
  - 从 Base64 直返改为解密/空值回退
- 后一轮修复点转移到 `ResourceGroupLoadBalancer.getInstanceResponse()`
  - 从 `retainAll(...)`
  - 改成直接 `jobNodes = rgManager.getInstancesForProject(project)`
- 这说明防线从“输入编码处理层”前移到了“最终目标选择层”

#### chain confidence
- `tightening-family`

#### allowed phrasing
- 可以写：
  - “同仓库 repeated fix family”
  - “防线从 decode/scheme 层前移到 routing/target-selection 层”
  - “later tightening closer to requester”
- 不应写：
  - “已证实某个 payload 绕过了前一修复”

#### reference evidence
- `discussion`: `KYLIN-5644`, `KYLIN-6082`, `lists.apache.org`, `openwall`
- `advisory`: `NVD`
- `code`: commit + PR

#### code evidence
- `NBasicController.decodeHost(...)`
- `ResourceGroupLoadBalancer.getInstanceResponse()`

#### reference-aware 伪代码
```text
V0
resolved_target = resolver.normalize(input_url)
decoded_target = resolver.decode(resolved_target)
requester.fetch(decoded_target)

Fix-1
resolved_target = resolver.normalize(input_url)
decoded_target = resolver.decode_and_decrypt(resolved_target)
requester.fetch(decoded_target)

Later hardening / Follow-up tightening
allowed_targets = policy.targets_for_project(project)
resolved_target = router.pick(allowed_targets)
requester.fetch(resolved_target)

What the chain proves
the repair focus moved from input encoding semantics
to constraining the final requester-side target set
```

## 单次修复模式

### EasyAdmin

`case_id`: `GHSA-33pg-fqx9-9w66`

#### all references reviewed
- `NVD`
- `Gitee commit`
- `Gitee issue I98YSR`
- `VulDB` 2 条

#### reference summary
- `Gitee issue I98YSR` 标题是：`EasyAdmin 未授权 SSRF/图片文件读取`
- 当前 references 能证明这个 case 的历史语义是：
  - 明确的 SSRF / 文件读取问题
  - 有一个对应 issue 和一个对应修复提交
- references **没有** 额外显示它是某个前次修复的 follow-up

#### code summary
- `thumbnail(...)` 在请求路径前增加两层护栏：
  - `http/https` scheme 检查
  - `new URL(url).getHost()` 白名单检查

#### chain confidence
- `single-fix`

#### allowed phrasing
- “一次性修复样本”
- “scheme + host allowlist 的正面例子”

#### reference-aware 伪代码
```text
V0
resolved_target = input_url
requester.fetch(resolved_target)

Fix-1
resolved_target = resolver.normalize(input_url)
if scheme(resolved_target) not in allowed_scheme:
    reject()
if host(resolved_target) not in allowed_host:
    reject()
requester.fetch(resolved_target)

完整修复应满足
validator and requester must resolve the same host semantics
```

### 其他 single-fix 代表
全量 references 复核后，下列保留 case 目前更适合当“单次修复模式样本”，而不是历史链：
- `GHSA-9jg9-6wm2-x7p5` (`Karaf`)
- `GHSA-9cfq-v2hm-c3xr` (`Jenkins github-branch-source-plugin`)
- `GHSA-hp7x-282p-hhr9` (`Jenkins ecutest-plugin`)
- `GHSA-mhrg-94vw-45c5` (`Spring AI`)
- `GHSA-42fc-7w97-8vrc` (`XWiki macro-plantuml`)
- `GHSA-h976-6mc8-5w2v` (`PerfreeBlog`)

原因不是它们不重要，而是：
- references 支持它们是 SSRF 修复
- 也有 commit 或 advisory
- 但 references 不支持把它们写成“初修 -> 绕过 -> 再修”

## 汇总伪代码

在“研究全部 2576 个 case_id 及其 references”后，当前 corpus 能稳定支持的 Java Direct SSRF 修复模式，不是“遍地都是完整绕过链”，而是下面三种 reference-aware 结构：

### 模式 A：单次 request-path 前置校验
```text
V0
resolved_target = resolver.normalize(input_url)
requester.fetch(resolved_target)

Fix-1
if scheme(resolved_target) not in allowed_scheme:
    reject()
if host(resolved_target) not in allowed_host:
    reject()
requester.fetch(resolved_target)
```

### 模式 B：已有校验存在，但后续继续收紧其语义
```text
V0
requester.fetch(resolved_target)

Fix-1
if weak_string_guard(resolved_target):
    reject()
requester.fetch(resolved_target)

Later hardening
replace substring-based exception logic
with exact prefix / exact semantic matching
```

### 模式 C：防线从输入层前移到 requester 最终目标选择层
```text
V0
decoded_target = resolver.decode(input_url)
requester.fetch(decoded_target)

Fix-1
decoded_target = resolver.decode_and_decrypt(input_url)
requester.fetch(decoded_target)

Later hardening
allowed_targets = policy.targets_for_context(context)
resolved_target = router.pick(allowed_targets)
requester.fetch(resolved_target)
```

## 最终结果

基于全部 `2576` 个 case 和全部 `10062` 条 references，这一轮可以严格得到的结论是：

1. 当前 trace universe 主要是“非 Java / 非 direct-SSRF / 下游传播公告”集合，不能直接拿来抽 Java 修复模式。
2. 真正能进入 Java Direct SSRF 修复模式抽象的，是 `80` 个 `java_direct_ssrf = yes` case。
3. 其中真正具备足够代码证据的只有 `16` 个 `keep` case。
4. 在这 `16` 个里，当前 references 能稳妥支撑的是：
   - `tightening-family`
   - `single-fix`
5. 当前 **没有** 一条 Java family 能在“all references first”的严格口径下，被安全写成已证实的：
   - `Fix-1 -> Bypass-1 -> Fix-2`

这也是这次重做后最重要的变化：  
**结论变少了，但证据口径更干净了。**
